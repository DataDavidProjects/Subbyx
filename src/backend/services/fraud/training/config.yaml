# Training pipeline configuration

# Data paths
data:
  training_dir: "/Users/davidelupis/Desktop/Subbyx/data/04-modeling"

# Fraud label threshold (days)
modeling:
  fraud_threshold_days: 15

# Time-based split dates (used by create_training_data.py and checkouts API)
dates:
  train_end: "2024-09-01"
  val_end: "2024-10-01"
  test_start: "2024-09-01"
  test_end: "2024-10-31"

# MLflow configuration
mlflow:
  experiment_name: "fraud-detector"
  model_name: "fraud-detector"
  tracking_uri: "http://localhost:5002"
